<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <!-- adiciounou o css e foi na index.js fazer a alteração na const path e const use -->
    <title>To-Do List</title>
</head>

<body>
    <form action="/create" method="post"> <!-- rota q chama o create task nas rotas-->
        <input type="text" name="task" id="task" placeholder="Insira uma tarefa">
        <button id="addButton" type="submit">Adicionar</button> <!-- type submit faz a ação -->
    </form>


    <ul> <!-- adc o js e cria varias li dentro da ul -->
        <% tasks.forEach((task)=> { %>
            <li>
                <%= task.task %> | <%= new Date(task.date).toLocaleDateString('pt-BR', { weekday: 'long' ,
                        year: 'numeric' , month: 'long' , day: 'numeric' }) %>
            </li>
            <% }) %>
    </ul>


    <script src="script.js"></script>
</body>

</html>